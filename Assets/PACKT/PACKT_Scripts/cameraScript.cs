using UnityEngine;
using System.Collections;

public class cameraScript : MonoBehaviour {
	//camera is rotated in the view using the reticule, it follows the player
	//reticule needs to be culled out by the player

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
